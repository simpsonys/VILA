{
  "name": "voice-log-analyzer",
  "version": "1.0.32",
  "description": "Voice Interaction Log Analyzer - Utterance-level log analysis report tool",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "prebuild": "node scripts/update-build-time.js",
    "build": "npm run prebuild && electron-builder --win",
    "build:portable": "npm run prebuild && electron-builder --win portable",
    "test": "jest"
  },
  "author": "VD Division",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "electron": "^33.0.0",
    "electron-builder": "^25.1.0",
    "fast-check": "^4.5.3",
    "jest": "^30.2.0"
  },
  "dependencies": {
    "electron-updater": "^6.8.3",
    "jschardet": "^3.1.3"
  },
  "build": {
    "appId": "com.vd.voice-log-analyzer",
    "productName": "Voice Log Analyzer",
    "publish": {
      "provider": "github",
      "owner": "simpsonys",
      "repo": "VILA_Release"
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "icon.png"
    },
    "nsis": {
      "oneClick": true,
      "allowToChangeInstallationDirectory": false,
      "artifactName": "Voice-Log-Analyzer-Setup-${version}.${ext}"
    },
    "files": [
      "main.js",
      "preload.js",
      "index.html",
      "default_config.json",
      "build-time.json"
    ],
    "extraResources": []
  }
}
